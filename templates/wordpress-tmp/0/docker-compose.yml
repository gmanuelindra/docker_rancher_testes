wordpress:
  image: wordpress
  links:
    - db:mysql
  ports:
    - 80:80

wordpressa:
  image: wordpress
  links:
    - dba:mysql
  ports:
    - 80:80

wordpressb:
  image: wordpress
  links:
    - dbb:mysql
  ports:
    - 80:80

db:
  image: mariadb
  environment:
    MYSQL_ROOT_PASSWORD: example

dba:
  image: mariadb
  environment:
    MYSQL_ROOT_PASSWORD: example

dbb:
  image: mariadb
  environment:
    MYSQL_ROOT_PASSWORD: example